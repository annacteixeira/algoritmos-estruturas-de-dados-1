#include <stdio.h>
#include <stdlib.h>
#define PI 3.1415 // As constantes podem ser definidas com a diretiva #define

int main(){
    // As constantes também podem ser definidas com a palavra reservada const
    const int nro = 10; // A constante é uma variável que não pode ser alterada

    return 0;
}